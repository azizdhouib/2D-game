#include <stdio.h>
#include <stdlib.h>
#include <SDL/SDL.h>
#include <SDL/SDL_image.h>
#include <SDL/SDL_mixer.h>
#include "fonction.h"

SDL_Surface* scrolling(SDL_Surface *tab[],int p)
{
 
 if(p==0)
 {
   return tab[28];
 }
 else
 {
  return tab[29];
 }
}
